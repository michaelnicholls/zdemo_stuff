managed implementation in class zbp_mn_i_view unique;
strict ( 2 );

define behavior for zmn_i_view
persistent table zmntab
lock master
authorization master ( global, instance )
//etag master <field_name>
{
  field ( readonly : update ) id;
  create;
  update (features : instance);
  delete (features : instance);
  action (features : instance ) myaction;
}